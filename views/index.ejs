<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= titulo %></title>
</head>
<body>
    <nav>
        <div class="Titulo-site">
        <a href="/"></a><h1>Blog</h1></a>
        <p>Site</p>
        </div>
        <ul>
            <li><a href="/">Blog</a></li>
            <li><a href="/sobre">sobre</a></li>
            <li><a href="/blog/criar">Blog</a></li>
        </ul>

    </nav>
    <div class="conteudo-blog">
        <h2>Todo Blog</h2>
        <% if (blogs.length > 0 ) { %>
        <% blogs.forEach(blog => { %>

            <h3 class="titulo"><%= blog.titulo %></h3>
            <p class="conteudo"><%= blog.conteudo %></p>
            <% }) %>
            <% } %>
    </div>
</body>
</html>